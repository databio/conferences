{
  "name": "conferences",
  "version": "0.1.0",
  "private": true,
  "description": "AI-curated computational-biology conference + deadline API (conferences.databio.org)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "validate": "node scripts/validate.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.20.0"
  }
}
